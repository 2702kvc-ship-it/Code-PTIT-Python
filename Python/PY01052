import math

def snt(n):
    for i in range(2, int(math.sqrt(n)) + 1):
        if n % i == 0:
            return 0
    return n > 1

for _ in range(int(input())):
    s = input()
    tong = 0
    for i in s:
        tong += int(i)
    print("YES" if snt(tong) else "NO")

