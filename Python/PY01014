s = input().split()
a = int(s[0])
k = int(s[1])
n = int(s[2])

bmin = (-a) % k
if bmin == 0:
    bmin = k

ok = 0
for b in range(bmin, n - a + 1, k):
    print(b, end = " ")
    ok = 1

if ok == 0:
    print(-1)
